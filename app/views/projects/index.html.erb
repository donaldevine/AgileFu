<h1>My Projects</h1>

<table class="table">
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Target Start</th>
    <th>Target End</th>
  </tr>

<% @projects.each do |project| %>
  <tr>
    <td><%= link_to project.name, edit_project_path(project) %></td>
    <td><%= link_to project.description, edit_project_path(project) %></td>
    <td><%= link_to project.target_start_date, edit_project_path(project) %></td>
    <td><%= link_to project.target_completion_date, edit_project_path(project) %></td>
  </tr>
<% end %>
</table>

<br />

<%= button_to "New Project", new_project_path, :method => "get", :class => "btn btn-default" %>

